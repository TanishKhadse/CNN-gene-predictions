
// additional info includes p-value, potentially other related genes

export default function Entry({name}) {
    return (
        <div class="border-[1px] border-b-neutral-500 bg-neutral-200 px-3 py-4">
            {name}
        </div>
    )
}